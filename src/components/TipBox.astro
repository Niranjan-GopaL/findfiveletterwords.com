---
interface Props {
  tip: string;
}

const { tip } = Astro.props;
---

<aside class="mt-12 border border-hairline bg-card p-5">
  <p class="font-monoui text-[11px] uppercase tracking-caption text-muted">Strategy tip</p>
  <p class="mt-3 max-w-3xl text-body-strong">{tip}</p>
</aside>
