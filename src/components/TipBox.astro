---
interface Props {
  tip: string;
}

const { tip } = Astro.props;
---

<aside class="mt-12 rounded-2xl bg-cream p-5 text-blockink sm:p-6">
  <p class="font-monoui text-xs uppercase tracking-eyebrow">Strategy tip</p>
  <p class="mt-2 max-w-3xl">{tip}</p>
</aside>
